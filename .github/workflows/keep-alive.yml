name: Keep Supabase Alive
on:
  schedule:
    - cron: "0 8 */3 * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping
        run: curl -s "https://hlzaghdjqwxijogunans.supabase.co/rest/v1/prenotazioni?limit=1" -H "apikey: ${{ secrets.SUPABASE_KEY }}"
